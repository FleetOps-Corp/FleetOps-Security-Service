# =============================================================================
# API Gateway — Python dependencies
# Pinned to specific versions for reproducibility (SAD constraint: stability)
# =============================================================================

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Async HTTP client for proxying requests to downstream microservices
httpx==0.27.2

# JWT validation (SAD §7: JWT session token)
# PyJWT==2.9.0
PyJWT[crypto]>=2.8.0

# Rate limiting for FastAPI (SAD §6/7: efficiency tactic)
slowapi==0.1.9

# Data validation and settings management
pydantic==2.9.2
pydantic-settings==2.6.1

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==6.0.0
pytest-mock==3.14.0

# Static code analysis (style, imports, common errors)
ruff==0.15.16

# Static type checking
mypy==2.1.0

# Architecture dependency validation
import-linter==2.11

# Test client utilities
httpx==0.27.2
