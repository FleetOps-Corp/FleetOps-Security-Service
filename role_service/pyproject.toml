[tool.ruff]
line-length = 125
target-version = "py312"

[tool.ruff.lint]
select = [
    "E", # pycodestyle errors
    "F", # pyflakes
    "I", # import sorting
]

[tool.mypy]
python_version = "3.12"
strict = true
ignore_missing_imports = true