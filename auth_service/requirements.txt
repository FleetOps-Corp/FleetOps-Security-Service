# =============================================================================
# Auth Service — Python dependencies
# =============================================================================

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# ORM and async database driver (SAD: PostgreSQL + SQLAlchemy)
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10

# Migrations (SAD: Alembic)
alembic==1.14.0

# Password hashing (SAD §7: bcrypt)
passlib[bcrypt]==1.7.4
bcrypt==4.2.0

# JWT generation (SAD §7: session token)
# PyJWT==2.9.0

# Redis client for session cache (SAD pág. 9: session caching)
redis==5.2.0

# Data validation
pydantic==2.9.2
pydantic-settings==2.6.1
email-validator==2.2.0

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==6.0.0
pytest-mock==3.14.0

# Static code analysis (style, imports, common errors)
ruff==0.15.16

# Static type checking
mypy==2.1.0

# Architecture dependency validation
import-linter==2.11

PyJWT[crypto]>=2.8.0